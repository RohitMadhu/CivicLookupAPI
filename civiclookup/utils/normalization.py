import re
from typing import Optional

STATE_NAMES = { ... }  # (keep the full dict from original)

# ... (copy the normalize_state_legislative_district, format_district_label, etc. from original app.py)

def normalize_state_legislative_district(value: Optional[str]) -> str:
    # ... (improved version)
    pass

# Add other helper functions here